name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Neovim ${{ matrix.neovim }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        neovim:
          - stable
          - nightly

    steps:
      - name: Checkout replent
        uses: actions/checkout@v4

      - name: Install Neovim (${{ matrix.neovim }})
        uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: ${{ matrix.neovim }}

      - name: Install plenary.nvim
        run: |
          mkdir -p ~/.local/share/nvim/site/pack/test/start
          git clone --depth 1 https://github.com/nvim-lua/plenary.nvim \
            ~/.local/share/nvim/site/pack/test/start/plenary.nvim

      - name: Ensure tmux is available
        run: command -v tmux || sudo apt-get update && sudo apt-get install -y tmux

      - name: Run test suite
        env:
          # Opts in to the real tmux-pane end-to-end tests in
          # tests/tmux_backend_spec.lua. TMUX_TMPDIR isolates the default
          # tmux socket this run creates/destroys to a scratch dir, so it
          # can never collide with (or need to touch) any other session.
          REPLENT_TEST_TMUX: "1"
          TMUX_TMPDIR: ${{ runner.temp }}/replent-tmux-test
        run: |
          mkdir -p "$TMUX_TMPDIR"
          nvim --headless -u tests/minimal_init.lua \
          -c "lua require('plenary.test_harness').test_directory('tests/', { incremental = false, sequential = true, halt_on_error = true })" \
          -c "qa!"
